[pytest]
pythonpath = .
testpaths = tests
addopts = -q --import-mode=importlib
markers =
    integration: touches a real Postgres+pgvector DB (auto-skips if unavailable)
    e2e: full-stack end-to-end flow through the API
