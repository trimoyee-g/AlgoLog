-r requirements.txt
pytest==8.3.3
pytest-cov==5.0.0
numpy>=1.26  # already pulled in by torch, pinned loose so unit tests can build fake vectors
reportlab==4.2.5  # generates a real text-layer PDF so the ingest path is tested end to end
